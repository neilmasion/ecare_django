Download Req:
Vscode
Git
python

Command line (Powershell):
python -m venv venv
venv\Scripts\activate.bat
python -m pip install django
python -m django --version (Optional)
python -m pip install Pillow
cd eCare
python manage.py runserver
